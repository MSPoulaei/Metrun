# راهنمای انتشار و بیلد نسخه جدید (Release Guide)

این راهنما مراحل گام‌به‌گام آماده‌سازی، بیلد امضاشده (Signed Release) و انتشار برنامه **متران (Metrun)** در مارکت‌های ایرانی (کافه بازار، مایکت) و گوگل پلی را توضیح می‌دهد.

---

## ۱. تنظیم نسخه در `pubspec.yaml`

پیش از هر بیلد جدید، فایل `pubspec.yaml` را باز کرده و خط `version:` را به‌روزرسانی کنید:

```yaml
version: 2.0.1+6
```
* **عدد قبل از `+` (`2.0.1`)**: همان `versionName` است که به کاربر نمایش داده می‌شود.
* **عدد بعد از `+` (`6`)**: همان `versionCode` است (عدد صحیح که با هر انتشار باید حداقل ۱ واحد افزایش یابد تا اندروید اجازه آپدیت روی دستگاه را بدهد).

---

## ۲. بررسی فایل کلید امضا (`key.properties`)

مطمئن شوید فایل `android/key.properties` وجود دارد و حاوی مقادیر زیر است:

```properties
storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEYSTORE_PASSWORD
keyAlias=upload
storeFile=upload-keystore.jks
```

> [!CAUTION]
> از فایل `android/upload-keystore.jks` و رمز آن در یک مکان امن (مانند Google Drive یا فلش) نسخه پشتیبان بگیرید. در صورت گم شدن این کلید، هیچ‌گاه امکان آپدیت برنامه در کافه بازار یا مایکت وجود نخواهد داشت.

---

## ۳. دستورات ساخت بیلد رسمی (Release Build)

### الف) خروجی APK (مناسب کافه بازار و مایکت)
برای ساخت فایل نصبی کامل و امضاشده:
```bash
flutter build apk --release
```
* فایل خروجی در مسیر زیر تولید می‌شود:
  `build/app/outputs/flutter-apk/app-release.apk`

*(اختیاری - برای کاهش حجم دانلود کاربر بر اساس پردازنده):*
```bash
flutter build apk --release --split-per-abi
```

### ب) خروجی App Bundle (مناسب Google Play)
اگر قصد انتشار در گوگل پلی را دارید:
```bash
flutter build appbundle --release
```
* فایل خروجی: `build/app/outputs/bundle/release/app-release.aab`

---

## ۴. کنترل از راه دور (Remote Config via Gist)

برای تغییر وضعیت تبلیغات یا اعلان انتشار نسخه جدید بدون نیاز به آپلود APK جدید:

۱. در [gist.github.com](https://gist.github.com) یک Gist عمومی یا مخفی بسازید.
۲. محتویات فایل `gist_config.json` را درون آن کپی کنید:
```json
{
  "banner_enabled": false,
  "native_enabled": true,
  "latest_version": "2.0.0",
  "min_version": "2.0.0",
  "update_url": "https://cafebazaar.ir/app/com.mspco.metrun",
  "update_message": "نسخه جدید متران با تغییرات و بهبود مسیرها منتشر شد. برای عملکرد بهتر لطفاً به‌روزرسانی کنید."
}
```
۳. روی دکمه **Raw** کلیک کنید تا لینک مستقیم آن را بگیرید.
۴. این آدرس را در فایل `assets/config/ads.json` در کلید `remote_config_url` قرار دهید.

* **روشن کردن بنر:** هر زمان جایگاه بنر در ادیوری تایید شد، کافی است در گیست مقدار `"banner_enabled": true` بگذارید. برنامه به طور خودکار تاس ۵۰/۵۰ می‌اندازد.
* **اعلان آپدیت:** هر زمان نسخه جدید منتشر کردید (مثلاً `2.1.0`)، در گیست `"latest_version": "2.1.0"` را قرار دهید تا به کاربران نوتیفیکیشن و دیالوگ آپدیت با لینک بازار داده شود.

---

## ۵. رفتار برنامه هنگام قطع اینترنت بین‌الملل (ملی شدن اینترنت)

هنگامی که اینترنت بین‌الملل مسدود یا محدود شود:

| بخش برنامه | رفتار هنگام قطع اینترنت | توضیحات فنی |
| :--- | :--- | :--- |
| **مسیریابی آفلاین (Dijkstra)** | **۱۰۰٪ فعال و بدون اختلال** | تمام گراف مترو (`metrofa.txt`) و الگوریتم دایکسترا در خود اپلیکیشن اجرا می‌شود و نیازی به حتی یک بایت اینترنت ندارد. |
| **نقشه تعاملی مترو** | **۱۰۰٪ فعال و آفلاین** | تصویر باکیفیت نقشه در حافظه گوشی ذخیره است. |
| **مسیرهای برگزیده و اخیر** | **۱۰۰٪ فعال و آفلاین** | به صورت لوکال در حافظه گوشی ذخیره می‌شوند. |
| **سرویس آنلاین زمان‌بندی مترو** | **فعال در صورت در دسترس بودن سرور شهرداری** | سرور `metro.tehran.ir` روی دیتاسنترهای داخلی و شبکه ملی قرار دارد. اگر قطع باشد، اپ فوراً و بدون کرش به حالت آفلاین برمی‌گردد. |
| **تبلیغات ادیوری (Adivery)** | **فعال روی اینترنت ملی** | سرورها و CDNهای ادیوری داخل ایران (ابر آروان، شاتل، افرانت) قرار دارند و در صورت اینترنت ملی همچنان لود می‌شوند. اگر اینترنت کاملاً قطع باشد، بخش تبلیغات به صورت امن خالی می‌ماند. |
| **کانفیگ گیست (GitHub Gist)** | **فال‌بک به تنظیمات داخلی بدون توقف** | اگر گیست لود نشود، تایم‌اوت ۳ ثانیه‌ای دارد که به صورت موازی در پس‌زمینه اجرا می‌شود و هیچ تاخیری در کاربری برنامه ایجاد نمی‌کند؛ اپ با تنظیمات پیش‌فرض محلی کار می‌کند. |
